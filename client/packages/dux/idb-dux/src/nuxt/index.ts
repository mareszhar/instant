/**
 * `@mszr/idb-dux/nuxt` — h3/nitro/Nuxt server glue.
 *
 * `defineServerKit`, `defineAuthSyncHandler`, `defineWebhookHandler` —
 * thin h3 wiring over the admin and webhooks layers.
 *
 * Spec: `../../../docs/dux-spec-nuxt.md`. Scaffolded empty on purpose —
 * each export lands with its roadmap phase.
 */
export {}
